{
  "name": "real-time-functionality-with-trpcio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd client && npm run dev\" \"cd server && npm run dev\"",
    "install_deps": "concurrently \"cd client && npm install\" \"cd server && npm install\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
